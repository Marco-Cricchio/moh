name: ci
on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.19
      - run: bun install --frozen-lockfile
      - name: ink live render inside a pty
        run: |
          python3 - <<'PY'
          import pty, os, time, select, subprocess
          master, slave = pty.openpty()
          p = subprocess.Popen(["bun", "packages/tui/ink-hello.tsx"], stdin=slave, stdout=slave, stderr=slave, env=dict(os.environ, TERM="xterm-256color", COLUMNS="160", LINES="45"))
          os.close(slave)
          buf = b""
          end = time.time() + 5
          while time.time() < end:
              r, _, _ = select.select([master], [], [], 0.2)
              if r:
                  try: c = os.read(master, 65536)
                  except OSError: break
                  if not c: break
                  buf += c
          p.terminate()
          print("BYTES:", len(buf))
          print(buf[:3000])
          PY
